
let numeros = [1, 2, 3];

let resultado = numeros.map(function(item, index, array){
    return item * 2;
})

console.log(resultado);














/* 

    let numerosMultiplicados = numeros.map(function(item){
        return item * 2;
    })


    let numerosMultiplicados = numeros.map(item => item * 2);
    console.log(numerosMultiplicados);


    let numerosMultiplicados = [1, 2, 3].map(item => item * 2);


    let numeros = [1, 2, 3];

    let funcionMap = function(item){
        return item * 2;
    }
    
    let numerosMultiplicados = numeros.map(funcionMap);
 */



